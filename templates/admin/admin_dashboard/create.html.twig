{% extends 'base.html.twig' %}

{% block body %}
    <h1>{% if realisation is defined %}Modifier{% else %}Nouvelle{% endif %} Réalisation</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Enregistrer</button>
    {{ form_end(form) }}

    <a href="{{ path('app_admin_dashboard') }}">Retour au dashboard</a>
{% endblock %}
